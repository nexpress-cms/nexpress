/**
 * Default theme footer. Minimal — themes that want columns,
 * social icons, newsletter signup override `slots.footer` in
 * their own `defineTheme()` call.
 */
export function DefaultFooter() {
  return (
    <footer className="nx-site-footer">
      <nav>
        <ul />
      </nav>
    </footer>
  );
}
