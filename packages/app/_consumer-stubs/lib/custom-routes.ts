import { npDefineCustomRoutes } from "@nexpress/core/routes";

/** Consumer-owned code route catalog supplied by `src/lib/custom-routes.ts`. */
export const npCustomRoutes = npDefineCustomRoutes([]);
