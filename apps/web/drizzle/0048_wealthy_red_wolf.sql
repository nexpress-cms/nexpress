ALTER TABLE "np_agent_runs" ADD COLUMN "runtime_retry_at" timestamp with time zone;--> statement-breakpoint
ALTER TABLE "np_agent_runs" ADD CONSTRAINT "np_agent_runs_runtime_retry_check" CHECK ((("np_agent_runs"."origin"='gateway' and "np_agent_runs"."runtime_retry_at" is null) or ("np_agent_runs"."origin"='runtime' and (("np_agent_runs"."state"='waiting_retry')=("np_agent_runs"."runtime_retry_at" is not null)) and ("np_agent_runs"."runtime_retry_at" is null or ("np_agent_runs"."runtime_retry_at">="np_agent_runs"."queued_at" and "np_agent_runs"."runtime_retry_at"<"np_agent_runs"."deadline_at")) and ("np_agent_runs"."lease_until" is null or "np_agent_runs"."state" in ('running','verifying')))) is true);