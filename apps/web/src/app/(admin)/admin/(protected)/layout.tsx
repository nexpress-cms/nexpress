// Admin protected layout — actual implementation in
// `@nexpress/app/admin/protected-layout`. This file exists so
// Next.js's app router knows the route group has a layout; the
// real code is shared with `npx create-nexpress`-scaffolded
// projects so both render the same AdminShell chrome.
export { default } from "@nexpress/app/admin/protected-layout";
