---
"@nexpress/cli": patch
"create-nexpress": patch
---

Add a local theme authoring path with `nexpress create theme`, workspace-theme registration support in `theme add`, and fresh-scaffold `packages/themes/*` workspace coverage.
