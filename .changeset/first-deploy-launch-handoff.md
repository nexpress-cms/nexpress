---
"@nexpress/app": patch
"create-nexpress": patch
---

Add first-deploy launch handoffs to deploy plans so generated projects show the
host-specific starting URL or command before the migration, preflight, release,
and verification gates.
