---
"@nexpress/admin": patch
"@nexpress/app": patch
"@nexpress/core": patch
"@nexpress/plugin-sdk": patch
"@nexpress/theme": patch
---

Unify theme definitions, persisted overrides, Admin/import APIs, plugin theme reads and writes, OpenAPI, and CSS generation behind one validated nested token contract.
