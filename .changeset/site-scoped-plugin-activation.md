---
"@nexpress/admin": patch
"@nexpress/app": patch
"@nexpress/auth-pages": patch
"@nexpress/blocks": patch
"@nexpress/cli": patch
"@nexpress/core": patch
"@nexpress/next": patch
"@nexpress/plugin-oauth-github": patch
"@nexpress/plugin-oauth-google": patch
---

Scope plugin activation to one site through sparse activation overrides while
keeping configured code loaded process-wide. Gate dispatch, Admin and public
contributions, OAuth, transfer, ops, and scheduled executions by the same site
snapshot; preserve active owners across block, pattern, and template collisions.
