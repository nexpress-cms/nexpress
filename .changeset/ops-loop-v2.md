---
"@nexpress/app": patch
"@nexpress/cli": patch
"create-nexpress": patch
---

Expand the agent-operated ops loop with `ops preflight`, `ops health`, and
`nexpress ops doctor` handoffs for generated projects.
