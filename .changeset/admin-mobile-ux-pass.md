---
"@nexpress/admin": patch
"@nexpress/app": patch
"@nexpress/theme-default": patch
---

Improve admin mobile ergonomics by tightening narrow-screen shell spacing, simplifying topbar breadcrumbs on phones, and stacking editor actions on very small viewports. Also remove the default theme's member-status loading chrome so auth links do not flash on first render.
