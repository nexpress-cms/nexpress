---
"@nexpress/core": patch
"@nexpress/plugin-sdk": patch
"@nexpress/admin": patch
"@nexpress/app": patch
"@nexpress/plugin-shop": patch
"@nexpress/cli": patch
---

Add bounded binary plugin API responses and authenticated declarative Admin
downloads, then let Shop carrier adapters retrieve already-booked PDF, PNG, or
ZPL shipping labels through a PII-free, staff-audited, transient byte contract.
