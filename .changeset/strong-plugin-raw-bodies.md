---
"@nexpress/app": patch
"@nexpress/cli": patch
"@nexpress/core": patch
"@nexpress/plugin-sdk": patch
---

Add an opt-in, one-MiB exact raw-body contract for signed plugin API callbacks, including early route validation, runtime and discovery metadata, OpenAPI projection, bounded dispatch, and a safe CLI scaffold example.
