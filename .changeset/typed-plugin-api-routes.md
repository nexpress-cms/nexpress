---
"@nexpress/app": patch
"@nexpress/cli": patch
"@nexpress/core": patch
"@nexpress/plugin-sdk": patch
"create-nexpress": patch
---

Validate plugin API route definitions and handler results, share their typed request/response contract across core and the SDK, support bodyless HEAD dispatch for GET routes, and teach plugin doctor and new scaffolds the canonical static-route rules.
