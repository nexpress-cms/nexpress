---
"@nexpress/app": patch
"create-nexpress": patch
---

Add a scaffolded `pnpm run deploy:plan` command that prints target-specific deployment steps, environment variables, storage guidance, worker notes, and the final `doctor:prod` readiness gate.
