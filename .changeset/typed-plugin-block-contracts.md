---
"@nexpress/app": patch
"@nexpress/blocks": patch
"@nexpress/cli": patch
"@nexpress/next": patch
"@nexpress/plugin-sdk": patch
---

Validate plugin block definitions and prop schemas during definition and bootstrap, reject same-plugin duplicate types, report malformed and conflicting blocks in plugin doctor, and align the CLI scaffold and bundled block examples with the shared contract.
