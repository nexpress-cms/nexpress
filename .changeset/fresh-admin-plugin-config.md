---
"@nexpress/cli": patch
---

Update `nexpress create admin-plugin` to scaffold a `configSchema` auto-form instead of the legacy `admin.settings.fields` form, including the required `zod` dependency.
