---
"@nexpress/core": patch
"@nexpress/admin": patch
"@nexpress/app": patch
---

Run search reindexing through an exact durable collection job with fixed cursor batches, cross-worker serialization, bounded job progress, Admin collection selection, and non-blocking internal trigger outcomes while preserving the synchronous Core API.
