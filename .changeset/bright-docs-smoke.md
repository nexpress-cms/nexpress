---
"@nexpress/theme-docs": patch
---

Fix docs theme detail pages so seeded and authored post content renders from the built-in `content` rich-text field instead of falling back to an empty body placeholder.
