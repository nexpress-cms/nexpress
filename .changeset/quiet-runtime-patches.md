---
"@nexpress/core": patch
"@nexpress/wp-import": patch
"@nexpress/xliff": patch
---

Refresh the supported runtime dependency versions for S3 storage, JWT handling, PostgreSQL, XML parsing, and CSS tooling while applying the patched Nano ID security floor.
