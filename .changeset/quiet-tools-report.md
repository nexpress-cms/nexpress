---
"@nexpress/cli": patch
"create-nexpress": patch
---

Add a local-only `nexpress feedback` support report that exposes only installed NexPress versions, bounded runtime identifiers, and Doctor check IDs/states, then connect fresh scaffolds and the public feedback form to that redacted handoff.
