---
"@nexpress/app": patch
"create-nexpress": patch
---

Allow `pnpm run setup` to optionally create the first admin, activate a starter theme, and seed sample content, while preserving the `/admin/setup` continuation path when those fields are skipped.
