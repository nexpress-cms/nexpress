---
"@nexpress/app": patch
"@nexpress/cli": patch
"@nexpress/core": patch
"@nexpress/plugin-sdk": patch
---

Validate plugin scheduled task ids, five-field UTC cron expressions, handlers, duplicate registrations, and runtime results across the SDK and core host; report contract failures in plugin doctor and align the CLI scaffold and analytics example with the typed registry.
