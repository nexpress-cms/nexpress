---
"@nexpress/app": patch
"@nexpress/cli": patch
"@nexpress/core": patch
"@nexpress/next": patch
"@nexpress/plugin-sdk": patch
---

Validate plugin page route patterns and handlers during definition and boot, fully dispatch raw-path `locale: "none"` routes, report malformed and duplicate routes in plugin doctor, and add a typed public page-route scaffold.
