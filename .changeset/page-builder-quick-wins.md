---
"@nexpress/admin": patch
"@nexpress/blocks": patch
---

Page-builder quick wins (#467): conditional `visibleWhen` on prop fields (inverse of `hiddenWhen`), validation status badges on collapsed rows (`required` / `warning`), per-row collapsed state lifted to the orchestrator and persisted in `localStorage`, focus moves to a newly inserted block, and an empty-page state with one-click recommended starter blocks.
